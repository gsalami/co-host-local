# Ralph Progress Log
Started: Tue Jan 27 14:13:30 CET 2026
---

## Codebase Patterns
- Auth system uses simple username/password from env vars (AUTH_USER, AUTH_PASSWORD)
- User sessions stored in PostgreSQL sessions table (for WebSocket compatibility)
- Session data structure: `sessionData.passport.user` contains user ID (not claims.sub)
- All routes access user via `req.user.id` (not `req.user.claims.sub`)
- Express.User type extended in server/types.d.ts to match DbUser from shared/models/auth
- WebSocket auth extracts userId from signed session cookies
- Login page at /login, Landing page at / (for unauthenticated users)

## 2026-01-27 14:30 - ARCH-1
- What was implemented: Replaced Replit OpenID auth with simple local username/password authentication
- Files changed:
  - Created: server/auth.ts (new auth module), client/src/pages/Login.tsx, server/types.d.ts
  - Modified: server/routes.ts, client/src/App.tsx, client/src/hooks/use-auth.ts, client/src/lib/auth-utils.ts, client/src/pages/Landing.tsx, .env.example, package.json
  - Deleted: server/replit_integrations/auth/* (entire folder)
- **Learnings for future iterations:**
  - The sessions table is still needed for WebSocket authentication (session cookie validation)
  - Session structure is simpler now: `sessionData.passport.user` = userId string (was `sessionData.passport.user.claims.sub`)
  - All route handlers needed updating from `req.user?.claims?.sub` to `req.user?.id`
  - Express.User type must be augmented globally to include our DbUser fields
  - memorystore used for Express sessions, but sessions table still queried by WebSocket auth helper
  - Login endpoint is POST /api/login (not GET), logout is POST /api/logout
  - Landing page shown on / when not authenticated, login page on all other routes
---

## 2026-01-27 16:00 - ARCH-2
- What was implemented: Removed all Stripe billing and credits system integration
- Files changed:
  - Deleted: client/src/pages/Credits.tsx, client/src/pages/CheckoutSuccess.tsx, client/src/pages/CheckoutCancel.tsx, client/src/hooks/use-credits.ts, server/stripeClient.ts, server/webhookHandlers.ts
  - Modified: package.json (removed stripe + stripe-replit-sync), client/src/App.tsx (removed credit nav), server/routes.ts (removed all credit/usage/stripe routes), server/index.ts (removed Stripe init), shared/schema.ts (removed credit tables)
- **Learnings for future iterations:**
  - Stripe was deeply integrated - had dependencies in package.json, server files, client pages, routes, schema, and webhooks
  - Credit system had both frontend (UI components, hooks) and backend (routes, storage methods, DB tables) parts
  - Removed 3 DB tables: usageRecords, userCredits, creditPurchases
  - Storage.ts still has unused methods referencing removed types - will be cleaned up in ARCH-3 during SQLite migration
  - App now runs without billing restrictions - all routes and features are freely accessible
  - Removed credit-checking middleware (isAllowedEmail) that was gating API access
  - WebSocket handlers had credit deduction logic that needed removal
---
